# 2026-03-31 page-navigation-backfill plan

## 实施步骤

1. 新增前后端各自的 `navigateBack` 工具函数。
2. 修正 admin 端详情页、表单页、设置页的返回逻辑。
3. 修正 client 端详情页、表单页、统计页、设置页的返回逻辑。
4. 对少量缺失返回按钮的页面补充页头入口。
5. 运行前后端 type-check，确认没有引入新的类型问题。
6. 更新 OpenSpec change 与交接文档。

## 提交策略

- 提交 1：修复注册白名单路径问题。
- 提交 2：补齐页面统一返回入口。

如本地开发代理需要保留，则与导航提交一并提交；否则单独保留为本地改动。

## 验证点

- 注册接口不再返回 403。
- 目标页面点击返回时可正常回到模块页。
- 直接打开详情页 URL 时，返回行为仍然可用。
